# Config path (default: config.ini at project root)
BACKEND_CONFIG=

# OpenSearch container bootstrap password (required for docker compose)
# Set to match config.ini [opensearch] password for local dev
OPENSEARCH_INITIAL_ADMIN_PASSWORD=

# Frontend API URL (for local dev)
VITE_API_BASE=https://api.mathmex.com